'use strict';
module.exports = {
    type: 'httpserver',
    init: (b) => {
        
    },
    // incoming: (msg) => {
    //     return Promise.resolve(msg);
    // },
    // outgoing: (msg) => {
    //     return Promise.resolve(msg);
    // }
};
